<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ASCC</title>
    <link rel="icon" href="/img/favicon.ico">
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
</head>
<body>
    <div class="container-fluid">
        <div class="row header">
            <div class="col-auto">
                <div class="dropdown">
                    <span data-day="0" onclick="handleDayClick(this);">
                        <%= __("today") %>
                        <i class="fas fa-calendar" id="today-icon"></i> 
                    </span>
                </div>
            </div>
            <div class="col text-center" id="project-title">
                <%= __("project_title") %>
            </div>
            <div class="col-auto">
                <div class="dropdown">
                    <div class="dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-language"></i>
                    </div>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#" data-lang="en"><%= __("english") %></a></li>
                        <li><a class="dropdown-item" href="#" data-lang="zh"><%= __("chinese") %></a></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="content">
            <br>
            <div class="row">
                <% const days = ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday']; %>
                <% days.forEach((day, index) => { %>
                    <div class="col-4 col-md-2 mb-2">
                        <div class="dayButton" data-day="<%= index + 1 %>" onclick="handleDayClick(this)"><%= __(day) %></div>
                    </div>
                <% }); %>
            </div>
            <div class="row mt-4">
                <div class="col-12 col-md-6 order-md-12">
                    <input type="text" id="searchBar" placeholder="<%= __('search_Name') %>" onkeyup="searchNames()">
                    <ul id="nameList">
                        <% names.forEach(({ name, row, menu }, index) => { %>
                            <li data-index="<%= row %>" 
                                data-menu="<%= menu %>" 
                                onclick="handleNameClick(this)">
                                <span class="name"><%= name %></span>
                                <span class="menu-type"><%= menu %></span>
                            </li>
                        <% }) %>
                    </ul>
                </div>
                <div class="col-12 col-md-6 order-md-1 mb-4">
                    <h2 id="breakfastSection"><%= __("breakfast") %></h2>
                    <% breakfastMenu.forEach(({ title, image }, index) => { %>
                        <% if (index % 3 === 0) { %>
                            <div class="row">
                        <% } %>
                        
                        <div class="col-md-4 mb-4">
                            <div id="btn-b-<%= title %>" 
                                data-text="<%= title %>" 
                                onclick="handleMealClick(this, 'Breakfast');" 
                                class="panel pointer w-100" 
                                data-order="<%= index %>">
                                <img src="<%= image ? image : '/img/default.png' %>"
                                class="panel-img w-100" />
                                <p class="panel-title"><%= title %></p>
                            </div>
                        </div>
        
                        <% if ((index + 1) % 3 === 0 || index === breakfastMenu.length - 1) { %>
                            </div>
                        <% } %>
                    <% }) %>
                    <h2 id="lunchSection"><%= __("lunch") %></h2>
                    <% lunchMenu.forEach(({ title, image }, index) => { %>
                        <% if (index % 3 === 0) { %>
                            <div class="row">
                        <% } %>
                        
                        <div class="col-md-4 mb-4">
                            <div id="btn-l-<%= title %>" 
                                data-text="<%= title %>" 
                                onclick="handleMealClick(this, 'Lunch');" 
                                class="panel pointer w-100" 
                                data-order="<%= index %>">
                                <img src="<%= image ? image : '/img/default.png' %>"
                                class="panel-img w-100" />
                                <p class="panel-title"><%= title %></p>
                            </div>
                        </div>
        
                        <% if ((index + 1) % 3 === 0 || index === lunchMenu.length - 1) { %>
                            </div>
                        <% } %>
                    <% }) %>
                </div>
            </div>
        </div>
        <div class="row footer align-items-center">
            <div class="col button-container">
                <button onclick="window.location.href='/response'" class="actionButton"><%= __("responses") %></button>
                <button id="submitButton" class="actionButton disabled"><%= __("submit_order") %></button>
            </div>
        </div>
    </div>

    <script src="/js/settings.js"></script>
    <script src="/js/main.js"></script>
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
        crossorigin="anonymous"
    ></script>
    <script src="https://kit.fontawesome.com/a8eb5cb089.js" crossorigin="anonymous"></script>
</body>
</html>